/**
 * Build response with author signature
 * @returns {Object} Return the author signature
 */
function buildResponse() {
  return {
    name: 'Sergio',
    lastname: 'Martínez',
  };
}

module.exports = buildResponse;