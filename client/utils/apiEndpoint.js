const endpointUrl =  'http://localhost:3300/api/'

/**
 * exports the const for MELI endpoint for items and item
 */
export const GET_ITEMS = `${endpointUrl}items?q=`
export const GET_ITEM = `${endpointUrl}items/`